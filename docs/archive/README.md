# Archive

Historical planning material lives here to keep the repository root focused on the SDK.

These files are retained for project context only. The current public evaluator path is the root [README](../../README.md), examples, tests, and package metadata.
